import React from 'react';
import { Typography, Box } from '@mui/material';

const Home = () => {
  return (
    <Box>
      <Typography variant="h2">Welcome Home</Typography>
      <Typography variant="body1">Select "Heroes"  from the menu to start.</Typography>
    </Box>
  );
};

export default Home;